{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "dependencyDashboard": true,
  "extends": [
    "docker:enableMajor",
    "docker:pinDigests",
    "default:automergeDigest",
    "default:automergeMinor"
  ],
  "enabledManagers": [
    "dockerfile",
    "cargo",
    "custom.regex"
  ],
  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true
  },
  "packageRules": [
    {
      "description": "rust-version is a bare MSRV string with nowhere to hang a digest. docker:pinDigests matches on the docker datasource, so without this it queues a pinDigest update the regex below can never write, and the branch errors out on every run.",
      "matchManagers": ["custom.regex"],
      "pinDigests": false
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Keep Cargo.toml's rust-version in lockstep with the builder image tag. Same depName and datasource as the Dockerfile's rust image, so both land in one PR.",
      "managerFilePatterns": ["/(^|/)Cargo\\.toml$/"],
      "matchStrings": ["rust-version\\s*=\\s*\"(?<currentValue>\\d+\\.\\d+)\""],
      "depNameTemplate": "rust",
      "datasourceTemplate": "docker",
      "versioningTemplate": "docker"
    }
  ]
}
